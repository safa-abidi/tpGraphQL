enum Status {
    WAITING
    IN_PROGRESS
    CANCELLED
    DONE
}
type Todo {
    id: String!
    name: String!
    content: String!
    status: Status!
}
type Query {
    hello(name: String): String!
    getTodo(id:String!): Todo
}